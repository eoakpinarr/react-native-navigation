import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        padding: 10
    },
    input: {
        borderColor: 'black',
        borderWidth: 1,
        flex: 0.7,
        borderRadius: 10
    },
    text: {
        flex: 0.3,
        marginTop: 15,
        fontWeight: 'bold',
        color: 'black'
    }
})